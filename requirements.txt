# M.A.X 2.0 — Requirements
# Install with: pip install -r requirements.txt

# ── Core AI ────────────────────────────────────────────────────────────────────
groq>=0.9.0                     # Groq LLaMA 3.3 70B brain

# ── Voice ──────────────────────────────────────────────────────────────────────
pyttsx3>=2.90                   # Text-to-speech (offline, Windows SAPI5)
SpeechRecognition>=3.10.0       # Voice recognition (Google STT)
# PyAudio — install separately (microphone support):
#   pip install PyAudio           (Windows — pre-built wheel available)
#   Linux: sudo apt install portaudio19-dev && pip install pyaudio
#   Mac:   brew install portaudio && pip install pyaudio

# ── GUI ────────────────────────────────────────────────────────────────────────
ttkbootstrap>=1.10.0            # Themed tkinter widgets

# ── REST API ───────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0

# ── Web & Research ─────────────────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0          # HTML parsing for web research
lxml>=4.9.0                     # Faster BS4 parser

# ── Skills: Information ────────────────────────────────────────────────────────
wikipedia>=1.4.0
geocoder>=1.38.1                # IP-based location for weather
pyjokes>=0.6.0

# ── Skills: System & PC Control ───────────────────────────────────────────────
psutil>=5.9.0                   # CPU, RAM, battery stats
pyautogui>=0.9.54               # Screenshots, keyboard/mouse control
pygetwindow>=0.0.9              # Window switching and management
Pillow>=10.0.0                  # Image processing

# ── Skills: Media & Entertainment ─────────────────────────────────────────────
spotipy>=2.23.0                 # Spotify Web API control
yt-dlp>=2024.1.0                # YouTube download + metadata

# ── Skills: Messaging ─────────────────────────────────────────────────────────
pywhatkit>=5.4                  # WhatsApp Web automation

# ── Skills: Scheduling ────────────────────────────────────────────────────────
schedule>=1.2.0                 # Cron-style scheduler

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Windows-specific ──────────────────────────────────────────────────────────
pywin32>=306;    sys_platform == "win32"
pypiwin32>=223;  sys_platform == "win32"
comtypes>=1.4.0; sys_platform == "win32"
